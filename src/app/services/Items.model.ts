export interface Items {
    title: string;
    category: string;
    brand: string;
    color: string;
    slug: string;
    image: string; 
    id: number
  }

  export interface Categories {
    category: string;
  }